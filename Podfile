# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'Photorado' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Photorado
  pod 'SnapKit', '~> 5.0.0'
  pod 'Moya', '~> 14.0'
  pod 'Kingfisher'
  pod 'SwiftyGif'

  target 'PhotoradoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhotoradoUITests' do
    # Pods for testing
  end

end
